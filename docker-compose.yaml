version: '3'
services:
  mysql-db:
    image: mysql:latest
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      - MYSQL_DATABASE: db
    networks:
      - my-network
    volumes:
      - db:/var/lib/mysql

  spring-boot-app:
    image: medzrig/kaddem:latest # Utilisation de la variable d'environnement IMAGE_TAG  ${IMAGE_TAG}
    restart: on-failure
    ports:
      - "8089:8089"
    depends_on:
      - mysql-db
    environment:
      - MYSQL_ROOT_PASSWORD: ""
    volumes:
      - spring-boot-app:/app/logs

volumes:
  db:
  spring-boot-app: